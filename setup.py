# This file exists so Streamlit Community Cloud runs `pip install .`
# which installs the local `weldfatigue` package from src/.
# All actual config is in pyproject.toml.
from setuptools import setup
setup()
